<%= form_with(model: @todo, local: true) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.label :content %>
  <%= form.text_field :content %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
  <%= link_to "一覧画面にもどる", todos_path%>